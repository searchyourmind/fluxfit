{
  "name": "ExpenditureEstimate",
  "type": "object",
  "properties": {
    "estimated_expenditure": {
      "type": "number"
    },
    "confidence": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ]
    },
    "method": {
      "type": "string"
    }
  },
  "required": [
    "estimated_expenditure"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    }
  }
}