{
  "name": "WeightLog",
  "type": "object",
  "properties": {
    "weight_kg": {
      "type": "number"
    },
    "log_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "weight_kg",
    "log_date"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}