{
  "name": "TrainingLog",
  "type": "object",
  "properties": {
    "training_type": {
      "type": "string",
      "enum": [
        "chest",
        "back",
        "legs",
        "shoulders",
        "arms",
        "cardio",
        "swimming",
        "rest"
      ]
    },
    "duration_min": {
      "type": "number"
    },
    "steps": {
      "type": "number"
    },
    "notes": {
      "type": "string"
    },
    "log_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "training_type",
    "log_date"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}